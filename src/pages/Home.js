import React from 'react';
import Navbar from '../component/Navbar';
import Sidebar from '../component/Sidebar';

const Home = () => {
    return (
        <div>
           <Navbar/>
           <Sidebar />
        </div>
    );
}

export default Home;
